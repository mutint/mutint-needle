# aledb-needle's documentation. This component has no entry script of its own, so these pages
# are read through an assembled project's manual -- `./mutint docs` collects them.
#
# The top-level nav heading is how a page declares who it is for. `Using ALEdb` and
# `Extending ALEdb` are the two the manual merges; anything under another heading lands under
# "About this deployment" named for this component. See aledb_common/docs_manual.py.

site_name: aledb-needle
site_description: The mutation needle plot on an experiment's Overview

nav:
  - Using ALEdb:
      - The needle plot: using/needle-plot.md
