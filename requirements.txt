# Install mutint-core dependencies first:
#   pip install -r mutint-core/requirements.txt
#
# mutint-needle-specific additions (if any):
#
# There are none. The plot is d3 in the browser over two querysets here, and the
# library is vendored -- see mutint_needle/static/mutint_needle/VENDOR.md.
